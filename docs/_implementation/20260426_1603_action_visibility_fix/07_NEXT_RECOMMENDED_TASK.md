# Next Recommended Task

Extend assignment-aware capability data beyond indicator detail into list-oriented surfaces that still rely on coarser assumptions.

## Recommended next slice

1. Add lightweight capability fields to recurring queue and/or worklist row payloads where action buttons are exposed outside full detail.
2. Use those flags to disable or hide submit/review controls before the user opens the indicator.
3. Address the remaining backend-only endpoint on admin masters by adding edit UI if it is still a product requirement.

## Why this is next

- The primary AI visibility problem is solved.
- The biggest remaining permission inconsistency now sits on list/queue surfaces rather than the main detail workflow.
